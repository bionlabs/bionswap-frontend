import { Token } from "@bionswap/core-sdk";
import React from "react";

type Props = { tokens: Token[]; onBack?: () => void };

const ImportToken = (props: Props) => {
  return <div>ImportToken</div>;
};

export default ImportToken;
